module.exports = {
  reactScriptsVersion: "react-scripts",
  webpack: {
    configure: (webpackConfig, { env, paths }) => {
      // Przykład dostosowania konfiguracji webpack
      return webpackConfig;
    },
  },
};
